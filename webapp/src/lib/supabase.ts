// Simple Supabase client for database operations only
// No authentication - we use our custom auth system

// Export types for compatibility
export type { User, UserProfile, WaterLog, ActivityLevel, WeatherCondition } from '@prisma/client'